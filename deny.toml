# Configuration used for dependency checking with cargo-deny.
#
# For further details on all configuration options see:
# https://embarkstudios.github.io/cargo-deny/checks/cfg.html

[graph]
exclude-dev = true
targets = [
  { triple = "x86_64-unknown-linux-gnu" },
  { triple = "x86_64-apple-darwin" },
  { triple = "x86_64-pc-windows-msvc" },
  { triple = "aarch64-apple-darwin" },
  { triple = "wasm32-unknown-unknown" },
]

[advisories]
yanked = "deny"

[bans]
multiple-versions = "deny"
wildcards = "deny"

# List of allowed licenses.
[licenses]
allow = ["Apache-2.0", "MIT", "Unicode-DFS-2016", "Unicode-3.0"]
confidence-threshold = 0.9

[sources]
# TEMPORARY — remove before merging to main: the workspace tracks
# asciidoc-parser's `inline-ast` feature branch (asciidoc-rs/asciidoc-parser#1059).
# Restore `allow-git = []` when the dependency returns to crates.io.
allow-git = ["https://github.com/asciidoc-rs/asciidoc-parser"]
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
unknown-git = "deny"
unknown-registry = "deny"
