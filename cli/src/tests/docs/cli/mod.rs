//! Coverage of the crate's `cli` documentation module under
//! `docs/modules/cli/`, from the CLI's point of view.
//!
//! These pages document the `adoc` command line interface. This crate verifies
//! the invocations they show; the module grows a page per CLI topic.

mod index;
mod io_piping;
mod process_multiple_files;
mod set_safe_mode;
