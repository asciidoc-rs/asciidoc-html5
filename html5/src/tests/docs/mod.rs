//! Coverage of this crate's own documentation pages under `docs/`.
//!
//! These pages are descriptive documentation rather than a specification, so
//! their content is tracked as non-normative. The behavior a page advertises
//! is verified by ordinary tests in the same module.

mod api;
mod convert_your_first_file;
mod generate_html;
mod index;
mod safe_modes;
