//! Coverage of this crate's `generate-html` documentation module under
//! `docs/modules/generate-html/pages/`.
//!
//! These pages describe generating HTML5 with `asciidoc-html5`. The prose is
//! tracked as non-normative; the API invocations each page shows are verified
//! by ordinary tests in the same module. The module grows a page per topic.

mod custom_stylesheet;
mod default_stylesheet;
mod index;
mod stylesheet_modes;
